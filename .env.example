# LIAM Gmail MCP Server Configuration
# Copy this file to .env for local testing

# =============================================================================
# LIAM Backend Configuration
# =============================================================================

# LIAM API base URL (where MCP endpoints are hosted)
LIAM_API_BASE=https://us-central1-liam1-dev.cloudfunctions.net

# =============================================================================
# For Local Testing Only
# =============================================================================

# Your LIAM JWT access token for local testing
# Get this by completing LIAM's OAuth flow:
#   1. Go to LIAM web app and sign in
#   2. Copy the JWT from DevTools (Network tab → Authorization header)
#
# Note: When deployed to Dedalus, OAuth is configured in their dashboard
# pointing to LIAM's auth endpoints:
#   - Authorization URL: {LIAM_API_BASE}/authorize?account_type=mcp
#   - Token URL: {LIAM_API_BASE}/token
#   - Requires: client_secret (MCP_CLIENT_SECRET)
LIAM_ACCESS_TOKEN=
